pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = 'sincboflutter'
include ':app'

dependencyResolutionManagement {
    // Change repositoriesMode to allow project-level repositories present in build.gradle
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}
